# Core numerical / image IO
numpy>=1.26,<2.1
pillow>=10.0
opencv-python>=4.9
scipy>=1.11
tqdm>=4.66
requests>=2.31

# Deep learning (Mask2Former via HuggingFace). On Apple Silicon these wheels
# ship with MPS (Metal) support out of the box.
torch>=2.2
torchvision>=0.17
transformers>=4.40
accelerate>=0.29

# Interactive parking-disturbance app (Step 9): `streamlit run app.py`.
# Pulls in pandas, which src/disturbance.py itself does not need.
streamlit>=1.36

# Tests (tests/ -- pure numpy, no models or dataset needed)
pytest>=8.0

# --- Optional: single-image camera calibration -----------------------------
# GeoCalib (ECCV 2024) estimates focal length AND gravity direction from one
# image. Without it the pipeline falls back to EXIF and then to a blind FOV
# assumption, which on real phone footage misestimates the focal length by
# 35-45% and shears the reconstructed 3D geometry. Not on PyPI, so install
# from git:
#     pip install "git+https://github.com/cvg/GeoCalib.git"
# Code Apache-2.0, pretrained weights CC-BY-4.0. Everything degrades
# gracefully if it is absent (see src/calibration.py).

# --- Optional: click-to-select in the Step 9 app ---------------------------
# Streamlit cannot report image click coordinates on its own. With this the
# vehicle can be picked by clicking it; without it the numbered radio list in
# app.py does the same job, and the app is fully functional either way.
#     pip install streamlit-image-coordinates

# iOS migration tooling (tools/coreml_export_ofrsnet.py, src/ofrs/export.py).
# Like streamlit above, only used by that tool, not imported by the core
# training/inference pipeline (predict.py, disturbance.py). Not tested past
# torch 2.7 as of coremltools 9.0 -- this repo runs torch 2.13, which works
# for OFRSNet's conversion (verified) but should be re-checked after a torch
# upgrade.
coremltools>=9.0

# iOS migration tooling (src/mobile_instance/, tools/coreml_export_mobile_instance.py):
# YOLOv8-seg as the vehicle-instance student -- small, well-precedented Core
# ML export via Ultralytics' own `model.export(format="coreml")`.
# LICENSE NOTE: Ultralytics is AGPL-3.0, not permissive -- shipping an app
# built on it (rather than Ultralytics' paid enterprise license) means the
# AGPL copyleft terms apply to the app. Confirmed acceptable for this
# project's current research-only scope; revisit before any commercial
# release, or swap for a permissively-licensed detector at that point.
ultralytics>=8.0
